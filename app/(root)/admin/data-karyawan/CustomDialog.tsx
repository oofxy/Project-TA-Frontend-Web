"use client";

import Header from "@/components/Header";
import React from "react";

const CustomDialog = () => {
  return (
    <div>
      <Header onClick={() => {}} buttonLabel="Add Data" />
    </div>
  );
};

export default CustomDialog;
